export const X_DIM_RGB = '21, 32, 43'
export const X_DIM_BG = `rgb(${X_DIM_RGB})`

// Surfaces in the original Dim theme.
export const X_DIM_BG_HOVER = 'rgb(30, 39, 50)'
export const X_DIM_BG_ELEVATED = 'rgb(39, 51, 64)'
export const X_DIM_BACKDROP = 'rgba(21, 32, 43, 0.85)'

// Lights Out uses this for some section cards; we map it to Dim hover.
export const X_LIGHTS_OUT_ELEVATED_BG = 'rgb(24, 24, 27)'

// Dim theme text/border colors (mostly for secondary UI).
export const X_DIM_TEXT = 'rgb(139, 152, 165)'
export const X_DIM_BORDER = 'rgb(56, 68, 77)'

export const X_ACCENT = 'rgb(29, 155, 240)'
